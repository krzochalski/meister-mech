import mechanics from './exemplaryData/mechanics';
import stations from './exemplaryData/stations';

const collection = {
  stations,
  mechanics,
};

console.log(collection);

export default collection;
